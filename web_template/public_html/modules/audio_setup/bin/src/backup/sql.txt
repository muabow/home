REATE TABLE audio_client (
		`use`       TEXT   NOT NULL,
		`stat`       TEXT   NOT NULL,
		`actStat`       TEXT   NOT NULL,
		`operType`       TEXT   NOT NULL,
		`protocol`       TEXT   NOT NULL,
		`castType`       TEXT   NOT NULL,
		`buffer_sec`   INTEGER   NOT NULL,
		`buffer_msec`   INTEGER   NOT NULL,
		`volume`      INTEGER   NOT NULL,
		`redundancy`   TEXT   NOT NULL,
		`deviceName`   TEXT   NOT NULL,
		`default_unicast_ipAddr` INTEGER   NOT NULL,
		`default_unicast_port` INTEGER   NOT NULL,
		`default_unicast_rep_ipAddr` INTEGER   NOT NULL,
		`default_unicast_rep_port` INTEGER   NOT NULL,
		`default_multicast_ipAddr' INTEGER   NOT NULL,
		`default_multicast_port` INTEGER NOT NULL,
		`change_unicast_ipAddr` INTEGER NOT	NULL,
		`change_unicast_port` INTEGER NOT NULL,
		`change_unicast_rep_ipAddr` INTEGER NOT NULL,
		`change_unicast_rep_port` INTEGER NOT NULL,
		`change_multicast_ipAddr` INTEGER NOT NULL,
		`change_multicast_port` INTEGER NOT NULL
);

CREATE TABLE audio_server (
		`use`       TEXT   NOT NULL,
		`stat`       TEXT   NOT NULL,
		`actStat`       TEXT   NOT NULL,
		`protocol`       TEXT   NOT NULL,
		`castType`       TEXT   NOT NULL,
		`encode`       TEXT   NOT NULL,
		`pcm_sampleRate`   INTEGER   NOT NULL,
		`pcm_channels`   INTEGER   NOT NULL,
		`pcm_chunkSize`   INTEGER   NOT NULL,
		`mp3_sampleRate`   INTEGER   NOT NULL,
		`mp3_bitRate`   INTEGER   NOT NULL,
		`mp3_chunkSize`   INTEGER NOT NULL,
		`deviceName`   TEXT   NOT NULL,
		`queueCnt` INTEGER   NOT NULL,
		`clientCnt` INTEGER   NOT NULL,
		`bufferRate` INTEGER   NOT NULL,
		`playMode' TEXT   NOT NULL,
		`fileName` TEXT NOT NULL,
		`operType` TEXT NOT NULL,
		`default_ipAddr` TEXT NOT NULL,
		`default_port` INTEGER NOT NULL,
		`change_ipAddr` TEXT NOT NULL,
		`change_port` INTEGER NOT NULL
	);

